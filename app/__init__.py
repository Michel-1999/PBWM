"""The Bank — AI prototype application package.

Modules
-------
bank_profile : Fixed, documented facts about "The Bank" (single source of truth).
fixtures     : The Mueller family seed data (portfolios, trades, CRM, messages).
state        : Shared st.session_state backbone + mutator helpers.
score        : Rule-based, transparent Inheritance Engagement Score + NBAs.
gemini        : Grounded, guardrailed Gemini chat/helper layer (with mock mode).
ui           : Brand theme (CSS) + reusable widgets (header, cards, avatar, chat).
"""
